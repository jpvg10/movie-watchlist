{
  "description": "Root package.json for Heroku deployment purposes only",
  "scripts": {
    "heroku-prebuild": "cd server && npm install && cd ../client && npm install && npm run build",
    "start": "cd server && npm run prod"
  }
}